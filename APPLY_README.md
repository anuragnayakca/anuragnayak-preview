# Apply V13 update

This is an overlay update for the current `anuragnayak-preview` repository.

It intentionally does **not** contain the whole site or delete any files. This protects articles and media added through Decap CMS after V12.

Copy the contents of this folder over the repository root, preserving the directory structure. Then review `git diff`, commit and push.

The existing public route `/services/business-insurance/` remains intentionally unchanged for URL/SEO continuity. The visible service name is now `Insurance for Business Owners`.
